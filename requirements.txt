blinker==1.9.0
click==8.2.1
colorama==0.4.6
Flask==3.1.1
flask-cors==6.0.1
# Flask-SQLAlchemy and SQLAlchemy are not needed if you only use sqlite3 directly (as in the latest app.py provided). 
# If you still use SQLAlchemy elsewhere, keep them; otherwise, you can remove them.
# Flask-SQLAlchemy==3.1.1
# SQLAlchemy==2.0.41
greenlet==3.2.3
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.2
# psycopg is only needed if using PostgreSQL, not if only using sqlite3.
# psycopg==3.2.9
typing_extensions==4.14.0
tzdata==2025.2
Werkzeug==3.1.3
gunicorn==21.2.0
